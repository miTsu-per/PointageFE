<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MobileApp.ClientMapPage"
             Shell.NavBarIsVisible="False">

    <Grid RowDefinitions="Auto, *, Auto">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#6a11cb" Offset="0.0" />
                <GradientStop Color="#4facfe" Offset="1.0" />
            </LinearGradientBrush>
        </Grid.Background>

        <Label Text="Position du Client"
               Grid.Row="0"
               FontSize="36"
               FontAttributes="Bold"
               TextColor="White"
               HorizontalOptions="Center"
               Padding="0,40,0,20"/>

        <WebView x:Name="MapWebView" Grid.Row="1" Margin="10"/>

        <Button Text="Retour"
                Grid.Row="2"
                Clicked="OnBackClicked"
                HorizontalOptions="Center"
                Margin="30"
                BackgroundColor="Transparent"
                TextColor="White"
                BorderColor="White"
                BorderWidth="2"
                CornerRadius="20"
                HeightRequest="40"
                WidthRequest="150"/>
    </Grid>
</ContentPage>
